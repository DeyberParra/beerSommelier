<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_search"
        android:title="@string/title_search_bar"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always|collapseActionView"
        />
    <item
        android:id="@+id/type_search"
        android:icon="@drawable/beer"
        android:title="@string/search_by_beer"
        app:showAsAction="always">

        <menu>
            <item
                android:id="@+id/search_by_beer_name"
                android:title="see beers"/>
            <item
                android:id="@+id/search_by_food"
                android:title="see foods"/>

        </menu>
    </item>

</menu>